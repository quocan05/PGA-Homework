export interface ILocationParams {
    id: string | number;
    name: string;
    pid: number | null;
  }
  export interface IStateParams {
    id: string;
    name: string;
    pid: number | null;
  }